/**
 * What a pet looks like where the character is a head and shoulders: the
 * dressing-up screen's swatches and the scrapbook. A pet sits beside the
 * stand, so a picture of the character with a pet looks the same as one
 * without; the pet is shown by itself instead.
 */
export const PET_ICONS: { [id: string]: string } = {
  none: '○',
  kitten: '🐱',
  puppy: '🐶',
  dragon: '🐲'
};
